# template-blank-reference — preview

The scaffolded page renders three feature cards above a footer, wired
to baseline tokens and the reference icon pack.

This file exists to anchor the `examples/` directory required by the
template-pack spec (`00_spec/template-pack.md` §3.1). The actual
preview screenshot ships with the launch site rather than the npm
tarball — see `04_docs/showcase/template-blank/` for the rendered
output.
